<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema claro forzado. AppCompatDelegate.MODE_NIGHT_NO asegura que nunca
         se active el modo oscuro independientemente de la preferencia del sistema. -->
    <style name="Theme.BSLottery" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>
</resources>
